module {
  func.func @normal_store_route(%value: i32, %output: memref<16xi32>) {
    %index = arith.constant 0 : index
    ADORA.kernel {
      memref.store %value, %output[%index] : memref<16xi32>
      ADORA.terminator
    } {KernelName = "normal_store_route"}
    return
  }
}
